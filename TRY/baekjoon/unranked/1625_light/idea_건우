문제에서 주어진 두 개의 연산법칙

하나는 행 전체의 전구 색깔을 다 바꾸는 거고
다른 하나는 열과 열을 바꾸는 건데
먼저 이 두 개의 연산으로는

초기의 어떤 행에서 켜져 있는 전구 수가 i개라면
마지막의 그 행에서의 전구가 켜진 개수는
i개 아니면 5-i개임

이 경우 말고는 절대로 있을 수가 없음
따라서 초기 전구와 최종 전구를 비교해서
만약 어떤 행의 켜진 전구 개수가 같다면
행연산은 0, 2, 4 같이 짝수 번 들어간거고
전구 개수가 차이나면 1,3,5같이 홀수번 행연산이 들어간거

문제에는 한 행 버튼을 최대 두 번 누를 수 있다는데
이 말은 서로 다른 행을 한 번씩 누를 수도 있고
같은 행을 두번 누를 수 도 있다는 건데

같은 행을 두번 누르는 건 아예 안누르는 거랑 똑같아서 제외할 수 있음
그리고 일단 초기 전구 상태에서 최종 전구 상태로 가는 방법을 알아냈으면
방법 내에서 버튼 누르는 순서는 마음대로 바꿔도 될 것 같음

그러면 이제 코딩을 할 수 있음
먼저 초기 전구와 최종 전구 행렬의 각 행들을 비교해서
개수 차이가 나는지 먼저 검토

가능한 경우의 수는
1. 하나도 차이가 안난다
2. 1개의 행에서 차이가 난다
3. 2개의 행에서 차이가 난다
그러면 한 행 버튼은 숫자 차이나는 행에다가 눌러주면 됨

그다음 행버튼 눌러놓은 전구 행렬과
최종 전구 행렬을 비교하면

각 열에 번호 붙여서 1,2,3,...,M이라고 지칭한다면
다른 열은 다 위치가 같은데 최대 3개의 열이 서로 순서가 바뀌어 있을 것
아 최대 3개는 아닐 수 있겠다
각각의 행 버튼을 최대 2번 누를 수 있다는 건가
그래도 차이나는 행을 일단 다 눌러놓고 시작하면 될듯


---

1. 최종전구행렬 빼기 초기전구행렬
2. 결과값 행렬의 각 행들의 합 구하기
3. 합이 0이 아닌 행들은 행 버튼 누른 거임
4. 3에서 구한 행버튼 누르는 행들 기억했다가 초기전구행렬에서 눌러서 중간행렬 도출
5. 5. 최종전구행렬의 1열부터 M열까지 순서 넣고
6. 중간행렬은 아마도 반드시 최종전구행렬에서 열 순서만 바꾼 거일거
그러면 예를 들어 최종전구행렬의 1열부터 M열까지를 각각 1,2,3,...,M으로 인덱싱하면